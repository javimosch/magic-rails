<%= form_for(@wallet) do |f| %>
  <% if @wallet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wallet.errors.count, "error") %> prohibited this wallet from being saved:</h2>

      <ul>
      <% @wallet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :lemonway_id %><br>
    <%= f.number_field :lemonway_id %>
  </div>
  <div class="field">
    <%= f.label :credit_card_display %><br>
    <%= f.text_field :credit_card_display %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
